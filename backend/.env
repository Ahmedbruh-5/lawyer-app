MONGO_URI=mongodb+srv://ahmedaamirgrw_db_user:ahmed11@cluster0.diayoey.mongodb.net/?appName=Cluster0


JWT_SECRET="advokate-desk"


CORS_ALLOWED_ORIGINS=http://localhost:5174,http://localhost:5173,https://advokate-desk-app.vercel.app/home

PORT=8000

# Penal code search DB: default import is backend/src/data/pakistan_penal_code.json (flat PPC rows).
# Optional: PENAL_CODES_IMPORT_PATH=/absolute/or/relative/path.json
# Legacy PPC JSON (chapters → sections) only: set PENAL_CODES_FILE=cleanPPC.json (must live under frontend/src/data/).
# PENAL_CODES_FILE=ppc_complete_new.json

# Statutes collection (separate from PenalCode): npm run seed:statutes — prefer backend/src/data/penalcodes.json path.
STATUTES_JSON_FILE=backend/src/data/penalcodes.json

# AI Lawyer RAG (FastAPI in src/AI Lawyer/). Must match Python .env RAG_API_KEY.
# Same machine: RAG runs on another port than Express (see MERN_DEVELOPER_HANDOFF).
# Use LAN IP only if Python runs on another PC: e.g. http://192.168.1.50:8001
RAG_SERVICE_URL=http://127.0.0.1:8001

RAG_API_KEY=ziU3Rmn4d4KrYD-4Tve9Vkbt8UJdOjxdd7IozpsB1SJAZ7S-Fbet2NILex53ZSOn

# Gmail SMTP for signup OTP:
# 1) Turn on 2-Step Verification, then 2) open App passwords: https://myaccount.google.com/apppasswords
# 3) Create a 16-character "Mail" app password and paste it below (NOT your normal Gmail password)
# 4) Optional: SMTP_PORT=587  (or 465 with SMTP_SECURE=true)
SMTP_USER=supportadvokatedesk@gmail.com
SMTP_PASS=jnjxkmauemdwjlmq


